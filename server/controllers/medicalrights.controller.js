/**
 * Get all posts
 * @param req
 * @param res
 * @returns void
 */
export function getMedicalRights(req, res) {
    let rights = [];
    if (false) {
      res.status(500).send(err);
    }
    res.json({ rights });
}
